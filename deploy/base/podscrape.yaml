apiVersion: operator.victoriametrics.com/v1beta1
kind: VMPodScrape
metadata:
  name: agc-entbannung
  namespace: agc
spec:
  podMetricsEndpoints:
    - port: http-metrics
      path: /metrics
      interval: 15s
  selector:
    matchLabels:
      app: agc-entbannung
